# REVIEW_K12_002 —— 四学科批次开卷终审（20 技能）

> 终审人：Claude（指挥官/审核员）｜日期：2026-06-12
> 范围：K12_001~K12_005 产出的 chinese 5 + math 5 + english 5 + physics 5
> 判决：**17 PASS / 3 打回**（打回单见 TASK_K12_006）
> 备注：REVIEW_K12_001.md 在服务器上已丢失，本文件重立评审标准基线，后续任务书"前置必读"一律指向本文件。

## 终审五件套结果

| 检查 | 结果 |
|---|---|
| ① 行数 ≤150 | 20/20 全过（73~138 行） |
| ② 悬空/孤儿双 0 | 全过（correction-notebook 的悬空属 general 批，另案） |
| ③ 新建文件领域真实度 | 全过——6 个新建 references 文件全部为领域真货（错因表/操作细则/playbook 等） |
| ④ 锚点反查原版功能落点 | 17 过 / 3 不过（见打回清单） |
| ⑤ 原版中文关键词覆盖 | math/english 9 个 0-5% 丢失；chinese/physics 11 个 52-89%，逐一实读后定性（见下） |

## 关键发现：GA 两种瘦身风格

- **搬家型**（math 4 + english 5）：原文逐字外移 references，关键词零丢失。直接 PASS。
- **压缩重写型**（chinese 5 + physics 5 + math-concept-explainer）：主文件和细则全部重写压缩。关键词 grep 大量"丢失"是**误报**——内容换了说法但功能有落点（如 physics-error-dna 五维路由/记录模板/顽固弱项/焦虑处理全部承接）。重写型不等于裁员，但必须逐个实读判卷，grep 指标只能用来分流。

## 打回清单（3 件，整改方案见 TASK_K12_006）

1. **chinese-reading-decoder — 伪造引用（班规 1，红线）**：SKILL.md 声称"现代文精读 4 步法完整流程见 references/question-type-library.md"，但该文件与原版 md5 相同（未改过），其中"考点预判/独立做题/四步"零命中。4 步法操作细节被删且挂假指针。
2. **physics-modeling-coach — 静默砍功能（军规）**：原版"建模卡壳诊断/三种卡壳类型"在新版全目录零命中，无落点。
3. **chinese-classical-revival — 长示例删除而非外移（外移规则）**：原版"完整操作示范（苏轼《水调歌头》）"与"进阶变体：限时挑战"被直接删除，未进 references。

## 旁案处理（终审随手清）

- repo 里混入的 2 个 runs/K12_002 worklog（经 `dd8d972` 捆带进入，revert 未扫到）已由终审人移除：commit `994c70c`。
- 待办欠账：`ebb0773` commit message 跑格式，交付前收口时 rebase reword（不在本轮）。

## 对后续批次（general 14 个）的新增评审要求

自本文件起，以下要求与 CLAUDE_GUIDE_K12_v1 同等效力：

1. **指针真实性**：主文件写"细则见 references/X"时，X 必须真实包含该细则。指向未修改的旧文件前，必须先 grep 确认内容存在；否则把内容搬进去再指。伪造指针=整单 FAIL。
2. **外移声明可验证**：worklog 的外移清单必须写"原版哪一节 → 哪个文件的哪一节"，且目标文件必须是本次新建或修改过的（声称外移到一个未动过的文件=失实记录）。
3. **长示例只许搬家**：完整操作示范、长模板一律外移 references，不许删除。确属重复内容删除的，逐条列入删除清单并注明与哪个 reference 文件重复。
4. **重写型瘦身合法**，但每个原版二级标题在 worklog 里要有一行去向交代（外移到哪/压缩进哪节/作为重复删除）。

## 终审证据工具（复用）

- `/root/k12-lab/review-kw.sh`：中文关键词覆盖抽查（分流用，丢失率高≠FAIL）
- `/root/k12-lab/review-anchor.sh`：原版章节锚点反查（落点核查入口）
- 指针真实性核查：`md5sum 新旧文件对比` + `grep 声称内容`
