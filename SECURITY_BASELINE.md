# SKILL 安全与隐私基线

## 适用范围

本基线适用于 `k12-education-skills` 仓库中的四个 Product Module、内部 playbook、
`k12-automation/scripts/nightline/` 运行时、学生模板、Obsidian/AGENTS 模板、日志、看板和导出报告。任何真正读取、写入或传输学生数据的实现，
都不能只依赖 `SKILL.md` 中的文字承诺。

目标：
- 降低默认激活、长期记忆、跨 module 共享、定时提醒、自动化触发的安全风险
- 统一所有 module/playbook 的授权边界、共享边界和用户控制入口

## 一、默认原则

所有 Product Module 与内部 playbook 都应遵守以下默认原则：

1. 普通请求先回答当前问题，再决定是否需要长期记忆或跨 module 联动；明确首次使用时，可先用用户当前提供的一份材料做无持久化的快速定位、形成会话内初版学习 DNA，然后立即回答真实学习任务。
2. 未经用户明确同意，不建立跨会话档案，不发送提醒，不共享给其他 module。
3. 只读取当前任务所需的最小必要字段，不读取完整历史档案。
4. 用户始终拥有查看、更正、删除、暂停、取消共享的控制权。

### 无副作用路由不等于数据共享

- `k12-learning` 只依据用户当前消息和 Capability Map 选择一个主 playbook，属于会话内无副作用路由，不需要额外授权。
- 路由阶段不得读取学习总控台、长期画像、错题历史、周报、家长反馈、提醒队列或外部系统数据。
- 只有读取历史数据、建立或更新长期档案、创建提醒、写入外部系统，或跨 module 传递历史摘要时，才进入对应授权门。
- 路由结果不得被当成用户同意安装、保存、共享或提醒；未安装 module 只能降级回答或提出安装建议。
- 例外仅限 `llm-wiki` 的 Obsidian 原生能力：任务实际需要且本机缺少能力时，可自动尝试官方 `kepano/obsidian-skills`。不得使用替代来源、覆盖已有目录或取得 vault/Learning State 的读取权限；网络失败必须降级并如实报告。

### 未成年人建档与运行层授权

- 学生模板必须默认 `authorized: false`；不得预填或推断监护人已经同意。
- 本地建档授权必须结构化记录主体（学生本人/监护人）、ISO 日期和方式（书面/口头/电子确认）；`authorized_by` 只保存对应低敏摘要，不保存身份证件、联系方式或签字原件。
- 向配置的模型服务发送数据必须单独授权，并记录提供方 origin、固定数据范围和授权日期；提供方或范围变化后旧授权失效。Mock 模式不外传。
- OCR 图片每次发送前都必须单独明确确认，并限制图片数量与大小；本地建档授权不能自动覆盖图片外传。
- 网页、脚本或批处理在建立长期目录、读取首页聚合、导出结果或运行分析前，必须校验对应授权；没有记录时应拒绝并说明补充方式。
- 撤回授权后，应停止后续分析，并按用户选择删除或导出相关 profile、archive、outbox、dashboard 和日志数据。

## 二、推荐文案

### 1. 涉及长期记忆

推荐写法：

```text
仅在用户明确同意后建立或读取长期档案。
未获同意时，只使用当前会话信息，不创建跨会话记录。
```

避免写法：

```text
必须激活此SKILL
每次对话开始前静默检查
自动存入DNA
永久记得你
```

### 2. 涉及提醒或调度

推荐写法：

```text
仅在用户明确要求设置提醒或同意接收该类提醒时启用。
用户可随时暂停、恢复、取消、调整频率。
```

避免写法：

```text
主动提醒
自动设置提醒
超过几天无对话自动唤醒
默认配置提醒
```

### 3. 涉及跨 module 共享

推荐写法：

```text
仅向声明过用途的 module 共享完成当前任务所需的最小字段摘要。
不传整份档案，不默认写回长期存储。
```

避免写法：

```text
任何 module 运行前优先读取DNA
拉取其他所有活跃 module
写入成长轨迹
默认同步到IM提醒
```

## 三、必须包含的控制入口

凡是涉及长期记忆、提醒、档案、履历、DNA、报告汇总的 module/playbook，文案中应提供至少一组可执行控制：

- 查看：`查看我的档案` / `查看我的提醒`
- 更正：`更正我的档案`
- 删除：`删除我的档案`
- 暂停：`这次不要记忆` / `暂停提醒`
- 共享控制：`不要共享给其他 module`

## 四、最小化记录原则

默认不记录以下信息：

- 真实住址、精确定位、身份证件、联系电话
- 财务、医疗、家庭纠纷等与学习任务无直接关系的信息
- 与当前学习任务无关的长篇闲聊细节

若用户主动提及，也应优先改写为低敏概括。

### 危机信号安全例外

自伤、自残、轻生、正在发生的人身伤害、严重暴力/虐待或无法保证当前安全等信号，不适用普通“低敏改写”规则。此时必须暂停学习任务、画像分析、周报和家庭看板美化，采用 `skills/k12-learning/references/crisis-referral-protocol.md`：

- 先用简短、不评判的话回应，只确认即时安全所需的信息，不诊断、不审问、不索取创伤细节；
- 即时危险时，要求停止独处并由安全、可信任的成年人陪伴，立即联系所在地紧急服务或前往最近急诊；
- 暂无即时危险时，也要尽快连接安全的可信成年人和合格专业支持；若监护人可能是伤害来源，改找其他安全成年人或当地保护服务；
- 不硬编码未经核验的热线号码；按用户所在国家或地区提供当前官方资源，且不得因等待地区信息延误即时处置；
- 不自动向任何第三方发送数据或声称已经转介。当前仓库没有危机处置事实的专用持久化 Schema，因此即使已有普通建档授权也不得写入 Learning DNA/State。未来若建立经评审的专用契约，仍须单独授权并只允许“已提示/已连接支持”等处置事实及日期，不得记录原话、方式、计划、创伤经过或诊断标签。

## 五、字段级 Schema 防护（写入约束）

凡是通过 JSON Schema 写入长期工作空间、共享档案或可导出结构（如 `dna-profile.schema.json` 等）的字段，Schema 本身必须承担第一道防线，而不能只依赖大模型层守门：

- **长度硬约束**：所有可能承载人话摘要的 `string` 字段（特别是 `parentSummary`、`factSummary`、`description`、`notes`、`usageNotes` 等），必须设置合理的 `maxLength`，推荐不超过 500 字符。超长输入一律在生成阶段收敛，不允许在存储阶段截断。
- **枚举/格式约束**：业务可枚举的状态、星期、等级、类型等字段必须使用 `enum` / `format`，避免自由文本写入。
- **description 写明禁止项**：高敏字段必须在 `description` 中显式声明禁止写入的内容类型（真实姓名、身份证件、联系方式、账户信息、医疗诊断、心理标签、家庭纠纷、财务细节等），让模型在生成时就能看到约束。
- **大模型层与 Schema 层双重把关**：SKILL 的"边界"章节中必须复述上述禁止项；Schema 校验作为最后一道兜底。模型层先做低敏改写，Schema 层再做结构和长度校验。
- **草稿态不得放宽**：`draft` / `sentStatus: draft` 等中间态不放松字段级防护，字段级约束适用于任何写入。
- **错误处理优先级**：当用户主动提供高敏信息时，模型应先提示"该项不会写入工作空间"，再询问是否改写为可记录的版本，不得静默丢弃也不得静默写入。

新增或修改 JSON Schema 时，应在 PR 自检中确认以上约束。

## 六、关键结论写入门槛

以下内容写入长期档案前，至少满足一项：

- 用户明确确认
- 连续多次对话或练习结果支持
- 小测、复述、费曼测试等方式验证通过

不要把一次情绪、随口评价或未经验证的推测直接固化为长期标签。

## 七、命名与措辞建议

敏感词并非绝对禁止，但应谨慎使用。若出现下列词语，建议同时补充授权与边界说明：

- `DNA`
- `档案`
- `长期记忆`
- `持久记忆`
- `跨次追踪`
- `提醒`
- `调度`
- `自动化`

如非必要，优先使用以下更稳妥的表达：

- `按需提示` 替代 `主动推送`
- `在用户同意后提醒` 替代 `主动提醒`
- `建议触发` 替代 `自动触发`
- `摘要字段` 替代 `完整档案`
- **价值/立场维度（政史地学科专项红线）**：用能力描述替代立场评判——用“价值论证缺失”（论证链不完整）替代“价值观偏差/立场错误/思想有问题”。涉及政治、历史、地理价值判断的 playbook，只训练价值论证构建，绝不评判学生个人政治立场或价值取向；该约束必须写入相关学科 playbook 的禁止行为表。

## 八、仓库维护要求

新增或修改 module/playbook 时，建议按以下顺序自检：

1. 是否存在“默认激活”“默认读取档案”“默认共享”的描述
2. 是否存在“自动提醒”“自动唤醒”“自动写回”的描述
3. 是否补充了授权、暂停、删除、取消共享入口
4. 是否把长期记忆限制为最小必要字段
5. 是否避免把第三次出错之类规则直接写成永久标签
6. **新增**：写入工作空间的字段是否在 JSON Schema 中设置了 `maxLength` / `enum` / `format` 等硬约束，并在 `description` 中显式声明高敏禁止项
7. 相关 module/playbook 的“边界”章节是否复述了 Schema 的字段级高敏禁止项

## 九、说明

本基线的目的不是削弱能力，而是把能力放在清晰、可控、可解释的模块边界内。
