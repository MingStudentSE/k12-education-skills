# 历史资料

本目录只用于追溯 V2 迁移过程和 genesis 学科设计。这里的命令、Skill 数量、目录路径、依赖关系、路由方式和执行要求均已失效，不是当前 AI 的执行指令或事实来源。

当前权威顺序为：

1. 根目录 `AGENTS.md` 与 `CONTEXT.md`；
2. `docs/adr/` 中已接受的架构决策；
3. 四个 Product Module 的 `SKILL.md`、当前 policy、Schema 与回归门禁；
4. 其他当前文档。

历史文件不得被运行时 module 引用，也不得用于恢复旧 Skill、handover、`depends_on` 或协调器语义。
